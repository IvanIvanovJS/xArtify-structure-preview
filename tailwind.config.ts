import type { Config } from "tailwindcss";


const config: Config = {
    // В v4 не са нужни content globs — JIT е винаги активен
    // Тук не дефинираме цветове; правим го в CSS чрез @theme
};


export default config;